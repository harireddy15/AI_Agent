# AI Trip Orchestrator

This project demonstrates multi-agent orchestration using MCP servers for travel planning (weather, flights, hotels, restaurants).

See `agents/` for agent definitions and `mcp/` for MCP server code.